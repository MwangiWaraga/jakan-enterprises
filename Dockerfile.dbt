FROM python:3.11-slim

RUN apt-get update \
    && apt-get install -y --no-install-recommends git build-essential libpq-dev \
    && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir dbt-core==1.10.9 dbt-postgres==1.9.0

WORKDIR /usr/app
